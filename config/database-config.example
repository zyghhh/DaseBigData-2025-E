# ========================================
# 数据库配置示例
# ========================================

# MySQL 连接信息
DB_HOST=node1
DB_PORT=3306
DB_NAME=stream_experiment
DB_USER=exp_user
DB_PASSWORD=password

# Root 用户密码（用于初始化数据库）
MYSQL_ROOT_PASSWORD=your_root_password

# JDBC URL
JDBC_URL=jdbc:mysql://node1:3306/stream_experiment?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# ========================================
# 修改指南：
# 
# 1. 修改 database/init.sh 中的 MYSQL_ROOT_PASSWORD
# 
# 2. 如需修改 exp_user 密码：
#    a. 修改 database/init.sql 中的密码
#    b. 修改 metrics-collector 源码中的密码
#    c. 重新编译项目
# 
# 3. 初始化数据库：
#    bash database/init.sh
#    
#    或手动执行：
#    mysql -h node1 -u root -p < database/init.sql
# 
# 4. 测试连接：
#    mysql -h node1 -u exp_user -ppassword stream_experiment
# ========================================

# ========================================
# 常用 MySQL 命令
# ========================================

# 连接数据库
# mysql -h node1 -u exp_user -ppassword stream_experiment

# 查看表结构
# DESCRIBE metrics;

# 查看统计视图
# SELECT * FROM v_latency_stats;
# SELECT * FROM v_duplicate_stats;
# SELECT * FROM v_comparison;

# 重置实验数据
# CALL sp_reset_experiment();

# 导出数据
# mysqldump -h node1 -u exp_user -ppassword stream_experiment > backup.sql
